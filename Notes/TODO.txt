fio.ZFS.Ext4.hwRAID.TestSuite.sh
  Flags for which kinds of tests to run
  if-condition wiping virtual drives if they exist instead of just presuming

lib/zfs
  selectively export only the pools that drives being tested are assigned to

lib/hwraid
  Identify and track the specific virtual drive (v0)
      that is going to be wiped and used

Command flags for testing instead of modifying comments on the main script

Flag/feature for counting the number of tests for a given config+profile
    and estimate for total time it'll take to complete